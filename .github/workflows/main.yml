name: A workflow for my Hello World file
on: push
jobs:
  build:
    name: Hello world action
    runs-on: ubuntu-latest
    environment: test
    env:
      MY_NAME: ${{ test.TEST }}
    steps:
      - uses: actions/checkout@v1
      - uses: ./action-a
        # with:
        #   MY_NAME: "Bram"
        # with: # Set the repo secret as an input
        #   MY_NAME: ${{ secrets.TEST }}
        with: # Set the repo secret as an input
          MY_NAME: ${{ env.MY_NAME }}